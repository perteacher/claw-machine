# 손인형뽑기 (Hand Claw Machine) — 통합 제작 & 배포 지시서
> **최신 통합본 (v6, 2026-06-21).** 5개 수정 + 확률 집기 + 실제 기계 외형/자동 배출 + 마일스톤(희귀도·도감 / 확률 수업 / 효과음·햅틱 / 난이도·코인 / 라벨 카드 편집) + 버그수정 라운드2(케이스 탈출·시작전 당첨) + 라벨 편집 정리 + 상점→상품 뽑기까지 반영한 "최종 목표 상태" 스펙.
> 새 Claude Code 세션에서 이 파일을 읽히고 **"이 파일대로 만들어줘"** 한 줄이면 됨.
> 진행: **caveman + cavecrew** (긴 구현은 서브에이전트에 위임하고 요약만 회수 → 메인 컨텍스트 절약).

---

## 0. 작업 규칙
- caveman + cavecrew로 진행.
- 결과물: **단일 정적 파일 `index.html` 하나.** 빌드 도구·번들러·npm 설치 없음.
- 모든 라이브러리는 CDN(importmap/script)로만 로드.

## 1. 목표
웹캠으로 손을 인식해 조종하는 **3D 인형뽑기**. 실제 인형뽑기 기계 외형의 유리 케이스 안에서,
손을 움직이면 집게가 따라오고, 손을 쥐면(핀치) 집게가 내려가 인형을 집고, 손을 펴면
집게가 **자동으로 배출구로 이동해 인형을 떨어뜨린다.** 카메라가 안 되면 마우스/터치 폴백.

## 2. 기술 스택 (전부 CDN)
- `three@0.160` — ESM importmap
- `cannon-es@0.20` — 인형 더미 물리
- `@mediapipe/hands` + `@mediapipe/camera_utils` — 손 랜드마크. **카메라 시작 시 lazy load.**
- ⚠️ `getUserMedia`/MediaPipe는 **HTTPS 필수** → GitHub Pages에서 정상. 로컬은 `localhost`만 허용.

---

## 3. 기계 외형 (실제 인형뽑기 기계)
- 직육면체 **캐비닛**: 빨간 지붕(top) + 흰/빨강 프레임 모서리 + **4면 반투명 유리벽** + 바닥.
- 윗면은 집게가 다니도록 **열려 있되**, 4면 유리벽으로 둘러싸여 인형이 절대 밖으로 못 나감.
- 전면 좌측 하단에 **상품 배출구(chute)** + 그 아래 수거함(밖에서 보이게).
- 장식: 전면 하단 컨트롤 패널에 둥근 버튼 3개(장식용), 빨간 띠 트림.
- 케이스 내부가 잘 보이도록 조명/시점 조정. 위에서 살짝 내려다보는 고정 시점.

## 4. 집게(claw) 모양
- 컴팩트한 허브(작은 원통/구) + **프롱 3개가 아래로 늘어뜨려져** 끝이 안쪽으로 모이는 크레인 집게.
- 각 프롱 = 위쪽 직선 + 아래쪽 안쪽으로 꺾인 갈고리(2 segment). 피벗은 허브 상단.
- open/close = 피벗 회전 각도: **열림 ≈ 28°(적당히만), 닫힘 ≈ 5°(끝이 거의 한 점)**.
- 중앙에 떠 있는 흰색 사각형/판 같은 잔여물 없을 것. 금색 메탈 재질.
- 천장에서 케이블(가는 cylinder)로 연결, 하강/상승 시 케이블 길이 동기화.

## 5. 인형(plush)
- 둥근 인형 **~26개**: 캔디색 랜덤, 작은 검은 눈 2개, 일부는 귀. 반지름 0.30~0.46.
- **스폰/더미는 집게 도달 범위(`clawX`/`clawZ`) 안**, 그리고 **chute 칸막이 영역은 제외**한 메인 영역에만.

---

## 6. 케이스 밀폐 + 배출구 분리 (인형이 배출구로 굴러 들어가는 문제 해결)
- 인형 더미는 유리 케이스(4벽 + 바닥)로 **완전 밀폐** → 굴러서 절대 못 나감.
- **배출구(chute)는 케이스 내부 한 코너에 '칸막이 벽'으로 분리**하고, 그 코너 바닥에 **구멍** → 아래 수거함으로 떨어지게.
- 칸막이 높이(`dividerH`): 인형이 굴러서는 **절대 못 넘되**, **집게는 위로 넘어갈 수 있는** 높이.
- 결과: 자유롭게 굴러다니는 인형은 chute에 못 들어가고, **오직 집게가 들어 옮길 때만** 들어감.
- 물리 콜라이더는 시각 벽과 정확히 일치. ball↔ground/wall restitution 낮게(안 튀게).

## 7. 조작
- **시작 오버레이**: "카메라로 시작" / "마우스·터치로 시작". 우상단 모드 토글 버튼.
- **카메라 모드**
  - 손 위치(landmark 9, x는 셀피 미러)로 집게 XZ 이동.
  - 좌우(x): `handXRange`를 `clawX` 전체로 정규화. 앞뒤(z): `handYRange`(중앙 밴드)를 `clawZ` 전체로 **확장 매핑** + 클램프 (앞뒤 인식 약한 문제 해결). z 방향이 직관과 맞는지 점검(필요시 반전).
  - 핀치 = `dist(thumbTip[4], indexTip[8]) / handSize < pinchThreshold`, `handSize = dist(wrist[0], lm[9])`.
  - 떨림 방지용 저역통과 스무딩(`smooth`).
- **마우스/터치 모드**: 드래그로 이동(레이캐스트 평면 좌표), 누르면 잡기 시작·떼면 release.
- **카메라 권한 거부/로드 실패 시 자동으로 마우스 모드 폴백** + 토스트.

## 8. 집게 하강/상승 속도
- 내려가 인형 잡는 데 **약 1초**, 다시 올라오는 데 **약 1초**.
- descend/ascend를 **시간 기반 보간**(`descendTime`, `ascendTime`)으로.

## 9. 잡기 로직 (확률적 집기 + 자동 배출) — state machine
`idle → descend(핀치 시작) → close(인형 선택 + 등급 굴림) → ascend → hold(핀치 유지·손 따라옴) → (release) deliver(자동 chute 이동) → drop → idle`

### 9-1. 확률적 집기 + 가변 악력 (실제 기계가 악력 조절하는 그 수법)
- close에서 반경 내 가장 가까운 인형 1개 선택 후 **등급**을 굴림:
  - **FIRM**(확실): 끝까지 안 놓침.
  - **WEAK**(헐거움): 잡지만 ascend/hold/**deliver 이동 중** 매 프레임 `weakSlipPerSec`로 슬립 판정 → 떨어짐.
  - **TEASE**(약올림): 살짝 들었다가 `teaseDropDelay` 후 곧 놓침.
- **pity**(사장 자비): 연속 실패(미배달)가 `pityThreshold` 넘으면 다음 집기는 FIRM 강제 후 카운터 리셋.
- 집은 인형 물리 바디는 sleep, 들고 있는 동안 매 프레임 집게 밑으로 위치 동기화.
- WEAK/TEASE는 프롱을 완전히 닫지 말고 살짝 열린 채로(헐겁게 물린 느낌). 슬립 순간 프롱 탁 벌리고 인형 흔들리며 떨어지게.

### 9-2. 자동 배출 (실제 기계처럼)
- hold 상태에서 손을 펴면(release) → 그 자리에서 놓지 말고, **집게가 자동으로 배출구(chute) 위로 이동** → 도착하면 스스로 인형을 놓음.
- `deliver` 이동 중에는 **사용자 입력(손/마우스) 무시**.
- chute에 떨어진 인형은 구멍으로 빠져 수거함에 → **점수 +1**.
- 점수는 **chute 통과 시에만** +1. 배달 성공하면 연속실패 카운터 0 리셋. 슬립/약올림으로 케이스 바닥에 떨어지면 점수 없음 + 연속실패 +1.

## 10. HUD
- 좌상단 "담은 인형 N" 카운터, 하단 조작 힌트, 중앙 상단 토스트.
- 카메라 모드 시 우하단 작은 웹캠 프리뷰(미러). 다크 글래스모피즘.

---

## 11. 튜닝 상수 (파일 상단 `CFG` 객체로 모을 것)
```
// 유리 케이스 내부
caseX:[-2.5, 2.5], caseZ:[-2.0, 2.0], caseH:3.6,
// 집게 도달 범위 (케이스 안쪽, 인형 반경 고려)
clawX:[-2.2, 2.2], clawZ:[-1.7, 1.7],
restY:3.2, grabY:0.5, topY:4.6,
descendTime:1.0, ascendTime:1.0,        // 하강/상승 각 ~1초
grabRadius:0.6,
// 배출구(코너 칸막이 + 바닥 구멍)
chuteX:-1.7, chuteZ:1.4, chuteHalf:0.6, dividerH:0.85,
// 인형 스폰(집게 도달 범위 안, chute 코너 제외)
spawnX:[-1.9, 1.9], spawnZ:[-1.5, 0.9], nBalls:26,
// 카메라 손 인식
pinchThreshold:0.55, handXRange:[0.1, 0.9], handYRange:[0.25, 0.78], smooth:0.25,
// 확률 집기
grabFirmRate:0.35, grabWeakRate:0.40,   // TEASE = 나머지(0.25)
weakSlipPerSec:0.6, teaseDropDelay:0.35, pityThreshold:4
```

## 12. 기타
- 모바일 세로 비율 대응, `devicePixelRatio` 2 캡, `touch-action:none`.
- 파일명은 **반드시 `index.html`** (루트 URL 직링크).
- 마우스/카메라 모드 둘 다 위 모든 동작 동일 적용.

---

## 13. 배포 — GitHub Pages
- 계정: **perteacher** (가명). 새 **public** 레포 `claw-machine`.
- 최종 URL: **https://perteacher.github.io/claw-machine/**
- ⚠️ 인증(`gh auth`)·로그인은 **사용자가 직접**. CC는 자격증명/비밀번호 입력하지 말 것.

### A. gh CLI 경로
```bash
git init
git add index.html
git commit -m "feat: hand claw machine"
gh repo create perteacher/claw-machine --public --source=. --remote=origin --push
gh api -X POST repos/perteacher/claw-machine/pages \
  -f "source[branch]=main" -f "source[path]=/" 2>/dev/null \
  || echo "→ Settings>Pages에서 수동 활성화 필요"
```

### B. 수동 경로
1. GitHub에서 public 레포 `claw-machine` 생성
2. `index.html` 푸시
3. Settings → Pages → Source: **Deploy from a branch → main / (root)** → Save
4. 1~2분 뒤 https://perteacher.github.io/claw-machine/ 확인

### C. 배포 후 체크
- [ ] https로 열리고 카메라 권한 요청이 뜸
- [ ] 모바일 세로에서 레이아웃·터치 정상
- [ ] three / cannon-es / mediapipe CDN 로드 OK (콘솔 에러 없음)
- [ ] 인형이 케이스 밖/배출구로 굴러나가지 않음
- [ ] 집게 하강·상승 각 ~1초, release 시 자동 배출 동작
- [ ] 확률 집기(FIRM/WEAK/TEASE) + pity 동작

---

## 14. (선택) 다음 단계 메모
- **앱인토스**: 카메라 테스트 검증부터. Pages 안정화 후 Toss 웹뷰 카메라 권한·세로비율 호환성 점검.
- 확장 후보: 제한시간/라이프, 효과음·햅틱, 인형 희귀도, 난이도 프리셋(쉬움/현실).
- 블로그(perteacher)용: 제작 과정 + CC 프롬프트로 dev-diary 1편.

---

## 15. 추가 마일스톤

### M1 — 인형 희귀도 + 도감 (먼저 진행)
- 인형을 '타입(DOLLS)'으로 정의하고 희귀도 부여: **일반 / 레어 / 유니크**.
- 희귀도별 속성: 출현 비중(weight), 잡기 난이도 배율(grabMul, 희귀할수록 ↓), 점수(points), 외형(glow/메탈/파티클).
  - 일반: 매트·흔함 / 레어: 살짝 발광·메탈 / 유니크: 강한 emissive + 반짝임(스파클) + 드묾.
- 스폰 시 weight로 타입 분포 결정.
- **잡기 확률에 grabMul 적용**: 희귀할수록 FIRM 확률↓(WEAK/TEASE로 치우침) → 자연히 뽑기 어려움.
- 점수는 희귀도별 차등(예: 1 / 3 / 10).
- **도감(Collection) 패널**(토글 버튼): 전 타입 나열, 수집=풀컬러+이름+획득수 / 미수집=실루엣+"???".
- **영속화**: 도감·획득수를 localStorage 저장(새 방문에도 유지). ※ GitHub Pages 자체 호스팅에선 localStorage 정상 동작. PII 아님.
- 레어/유니크 획득 시 특별 토스트 + 연출(✨).
- 전부 CFG(`RARITY` 테이블 + `DOLLS` 리스트)로 분리해 추가/조절 쉽게.

### M2 — 확률 수업 모드 (그 다음)
- 토글로 **'확률 수업 모드'** 패널.
- 실제 플레이 시도/성공/실패(슬립·약올림) 카운트 + 누적 성공률 표시.
- **그래프**: x=시행수, y=누적 상대도수(성공률) 라인 + **이론확률 p 수평 기준선** → 시행이 쌓일수록 p로 수렴(대수의 법칙)이 눈에 보임.
- **자동 시뮬레이션**: "+10 / +100 / +1000회" 버튼 → Bernoulli(p) 빠르게 굴려 차트 즉시 갱신. p는 슬라이더(`lessonP`, 기본 0.3).
- 차트는 **외부 라이브러리 없이 인라인 canvas/SVG**로(단일 HTML 유지). 리셋 버튼.
- 교육 포인트: **기대값·시행횟수·대수의 법칙** 데모. (게임 본 모델은 복잡하므로 수업 모드는 깔끔한 단일 확률 p 사용 → 수렴이 명확.)

### CFG 추가 (11번 블록에 합치기)
```
RARITY:{
  common:{weight:70, grabMul:1.0,  points:1,  glow:0.0},
  rare:  {weight:25, grabMul:0.7,  points:3,  glow:0.5},
  unique:{weight:5,  grabMul:0.45, points:10, glow:1.3},
},
DOLLS:[
  {id:'pinky',  name:'핑키',    rarity:'common', color:0xff7a9c, ears:true },
  {id:'lemon',  name:'레몬',    rarity:'common', color:0xffd23f, ears:false},
  {id:'sky',    name:'스카이',  rarity:'common', color:0x57c6ff, ears:true },
  {id:'mint',   name:'민트',    rarity:'common', color:0x8ce06b, ears:false},
  {id:'grape',  name:'그레이프',rarity:'rare',   color:0xc792ff, ears:true },
  {id:'coral',  name:'코랄',    rarity:'rare',   color:0xff9f5a, ears:false},
  {id:'aqua',   name:'아쿠아',  rarity:'rare',   color:0x6be0c8, ears:true },
  {id:'galaxy', name:'갤럭시',  rarity:'unique', color:0x9b5cff, ears:true },
  {id:'gold',   name:'골드스타',rarity:'unique', color:0xffd700, ears:false},
],
lessonP:0.3, simBatch:[10,100,1000]
```

### M3 — 효과음 + 햅틱 (먼저 진행)
- **Web Audio API 합성음**만 사용(외부 파일 0, 단일 HTML 유지). 짧은 비프/처프/노이즈 조합.
- 이벤트별 사운드: 집게 하강, 프롱 닫힘(집기), 슬립/약올림(맥빠지는 하강음), 배출 성공(상승 아르페지오).
- **희귀도별 성공음 차등**: 일반=짧은 띵 / 레어=2음 / 유니크=팡파레.
- **햅틱**: `navigator.vibrate` 지원 시 집기·슬립·성공에 짧은 진동(유니크는 길게). 미지원 기기는 무시.
- 우상단에 음소거 토글(🔊/🔇), 상태 localStorage 저장. 첫 사용자 제스처(시작 버튼) 때 AudioContext resume.
- CFG: `sfxEnabled`, `masterVolume`(0~1), 이벤트별 주파수/길이 상수.

### M4 — 난이도 프리셋 + 코인 모드 (그 다음)
- **난이도 프리셋**: 쉬움 / 현실 / 사장모드. 선택 시 확률·악력·pity를 일괄 전환.
  - 쉬움: grabFirmRate↑, pityThreshold↓, weakSlip↓ (잘 잡힘)
  - 현실: 현재 기본값
  - 사장모드: grabFirmRate↓, pityThreshold↑, weakSlip↑ (악랄)
- **코인 모드**(토글): 코인 N개로 시작, 집기 시도 1회당 코인 1 차감, 0이면 종료(결과 화면: 담은 인형·점수·획득 희귀도). 재시작 버튼.
- (선택) 타임어택: 제한시간 내 최대 점수.
- 프리셋·코인 설정은 시작 오버레이 또는 설정 패널에서 선택. 선택값 localStorage 저장.
- CFG: `PRESETS`(쉬움/현실/사장 각각의 확률 세트), `coinMode`, `startCoins`, `timeAttackSec`.

### CFG 추가 (11번 블록에 합치기)
```
sfxEnabled:true, masterVolume:0.6,
PRESETS:{
  easy:  {grabFirmRate:0.6,  grabWeakRate:0.3,  weakSlipPerSec:0.3, pityThreshold:2},
  real:  {grabFirmRate:0.35, grabWeakRate:0.4,  weakSlipPerSec:0.6, pityThreshold:4},
  boss:  {grabFirmRate:0.2,  grabWeakRate:0.4,  weakSlipPerSec:0.9, pityThreshold:7},
},
coinMode:false, startCoins:10, timeAttackSec:0   // timeAttackSec>0이면 타임어택
```

### M5 — 라벨 카드 (교사 직접 편집) ※M6에서 동작 변경됨, 아래 둘 함께 볼 것
- 인형 타입(DOLLS id)마다 **라벨(이름표) + 설명**을 매핑. 인형을 뽑으면 카드가 뜨고, 도감에 수집 항목이 쌓임. (라벨 = 과학 용어로도, 상품 이름으로도 쓸 수 있음 → M6 참조)
- **기본 상태 = 라벨 없음.** 자동으로 깔리는 시드 없음. 설정 안 하면 인형은 **기본 이름**으로만 표시.
- **데이터 우선순위**: 비어 있음(기본) → 교사가 편집/가져오기 한 값(localStorage). ※코드에 과학용어 자동 시드 넣지 말 것.
- **앱 내 편집 패널 (M6: 시작 화면에서만, 게임 중 불가)**:
  - DOLLS 목록을 표로, 각 행에서 **라벨·설명 직접 수정** → 저장 시 localStorage 반영.
  - 단원/세트명 필드도 편집.
  - **내보내기**: 현재 세트를 `.json`으로 다운로드(백업/공유) / **가져오기**: JSON 업로드로 일괄 적용.
  - **기본값으로 변경 = 라벨 전부 제거(빈 상태).** 과학용어 시드로 되돌리지 말 것.
- 비개발자도 클릭으로 수정 가능해야 함(코드 안 건드림). 잘못된 JSON 업로드 시 안내 토스트.

### (선택) 가져오기용 샘플 세트 — 동물과에너지 용어 ※기본값 아님, 원하면 import
```
SAMPLE_TERMS(동물과에너지):
{
  unit:'동물과에너지',
  map:{
    pinky: {term:'허파꽈리',  desc:'허파에서 기체 교환이 일어나는 작은 공기주머니'},
    lemon: {term:'토리',      desc:'콩팥에서 혈액을 거르는 모세혈관 뭉치'},
    sky:   {term:'콩팥단위',  desc:'오줌을 만드는 콩팥의 기본 단위(토리+세관)'},
    mint:  {term:'가로막',    desc:'가슴과 배를 나누는 근육, 호흡운동에 관여'},
    grape: {term:'숨관',      desc:'코·입에서 허파로 공기가 지나가는 관'},
    coral: {term:'숨관가지',  desc:'숨관이 갈라져 허파로 들어가는 관'},
    aqua:  {term:'허파순환',  desc:'심장과 허파 사이를 도는 혈액 순환'},
    galaxy:{term:'세포호흡',  desc:'세포가 산소로 영양소를 분해해 에너지를 얻는 과정'},
    gold:  {term:'요소',      desc:'단백질이 분해되며 생긴 암모니아를 간이 바꾼 노폐물'},
  }
}
```
※ 용어는 2022개정 신용어 기준(기관→숨관, 기관지→숨관가지, 횡격막→가로막, 네프론→콩팥단위, 사구체→토리, 폐순환→허파순환, 세포호흡 띄어쓰기X).

---

## 16. M6 — 버그수정 라운드2 + 라벨 편집 정리 + 상점→상품 뽑기

### [버그1] 시작 시 인형이 케이스(유리 통) 밖으로 굴러나가는 문제
- 인형은 케이스 내부에만, 벽에서 **인형 반지름 이상 안쪽**으로 떨어진 위치에 스폰. 스폰 영역이 내부를 넘지 않게.
- 스폰 초기 속도 0, 너무 높지 않은 데서 **살짝 시간차**로 떨어뜨려 깊은 겹침으로 벽 뚫는 현상(터널링) 방지.
- 케이스 4벽 물리 콜라이더를 시각 벽과 정확히 일치 + 충분히 높게, 모서리/이음새 틈 없게. 솔버 반복수↑.
- **안전장치**: 인형이 내부 밖으로 나가면 즉시 내부로 되돌리는 클램프.

### [버그2] 시작하자마자 굴러간 인형이 배출구로 들어가 '시작 전에 당첨'되는 문제
- 스폰 영역에서 **배출구 칸막이 코너 제외**(거기엔 인형 안 생기게).
- 칸막이를 인형이 굴러서는 **절대 못 넘는 높이**로(집게만 위로 넘김), 틈/낮은 구간 없게.
- 점수는 **'집게가 배출구 위에서 놓아 전달한 인형'일 때만 +1**(claw-delivered 플래그로 구분). 단순히 배출구에 들어갔다고 점수 주지 말 것.
- 시작 직후 인형이 다 가라앉는 **정착 시간** 동안 점수/배출 판정 비활성화, 정착 후부터 판정.

### [라벨 편집 정리] (M5 갱신과 동일 — 재확인)
- 편집 패널은 **시작 화면에서만**. 게임 진행 중 편집 불가(인게임 편집 토글 제거).
- 기본 라벨 없음 → 인형 **기본 이름**으로만 표시(과학용어 자동 시드 제거). 교사가 설정한 경우에만 카드 표시.
- **'기본값으로 변경' = 라벨 전부 제거(빈 상태로).** 과학용어 시드로 되돌리지 말 것.

### [상점→상품 뽑기] (M4 코인 모드 재활용)
- **명단·학생별 잔액·차감 기록 만들지 말 것.** 교사가 시작 전에 **코인 개수만 직접 입력**.
- 코인/점수 라벨을 **'상점'**으로 표기(상점 N개로 시작). 1회 집기 시도마다 상점 1 차감, 0이면 종료.
- 인형 = **'상품'**. 종료 시 결과 화면에 이번에 획득한 **상품 목록(이름/개수)** 표시.
- 상품 이름은 시작 전 **라벨 편집 패널**에서 설정(위 '라벨 편집' 그대로). 설정 안 하면 인형 기본 이름.
- 진행 중에는 코인 추가·편집 불가. **시작 전에만** 코인 수 + 상품 이름 설정.
- 단일 HTML 유지, 개인정보·네트워크 없음. 마우스/카메라 공통.

### CFG 영향
- M4의 `coinMode`/`startCoins` 재사용(라벨만 '상점'으로). 라벨(TERMS/상품)은 **비어 있음이 기본**.
- ‘상점’은 shop이 아니라 **칭찬점수(상점·벌점의 그 상점)** 를 뜻함.
